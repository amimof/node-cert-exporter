sudo: false
dist: stretch
language: go
go:
  - "1.11"
stages:
- test
- build
- deploy
before_install:
- make dep
jobs:
  include:
  - stage: test
    name: "Verify"
    script: 
    - make checkfmt
    - make fmt
    - make vet 
    - make gocyclo
    - make golint 
    - make ineffassign
    - make misspell
  - name: "Unit Test"
    script:
    - make test
  - name: "Integration Test"
    script:
    - make integration-test
  - stage: build
    script: 
    - make linux 
    - make darwin 
    - make rpi
    - make windows
  - stage: deploy
    name: "GitHub Releases"
    #script:
    #- cd /home/travis/
    deploy:
      provider: releases
      api_key: ${GITHUB_API_KEY}
      file: 
      - gopath/src/github.com/amimof/node-cert-exporter/out/node-cert-exporter-linux-amd64
      - gopath/src/github.com/amimof/node-cert-exporter/out/node-cert-exporter-darwin-amd64
      - gopath/src/github.com/amimof/node-cert-exporter/out/node-cert-exporter-linux-arm
      - gopath/src/github.com/amimof/node-cert-exporter/out/node-cert-exporter-windows-amd64.exe
      skip_cleanup: true
      draft: true
      on:
        tags: true