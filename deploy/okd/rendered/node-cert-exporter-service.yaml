apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: node-cert-exporter
  name: node-cert-exporter
  namespace: monitoring
spec:
  ports:
  - name: metric
    port: 9117
    targetPort: metric
  selector:
    k8s-app: node-cert-exporter
