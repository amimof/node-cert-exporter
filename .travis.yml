sudo: false
dist: stretch
language: go
go:
  - "1.11"
stages:
- test
- build
- deploy
before_install:
- make dep
jobs:
  include:
  - stage: test
    name: "Verify"
    script: 
    - make checkfmt
    - make fmt
    - make vet 
    - make gocyclo
    - make golint 
    - make ineffassign
    - make misspell
  - name: "Unit Test"
    script:
    - make test
  - name: "Integration Test"
    script:
    - make integration-test
  - stage: build
    script: 
    - make linux 
    - make darwin 
    - make rpi
    - make windows
  - stage: deploy
    name: "GitHub Releases"
    script:
    - cd /home/travis/gopath/src/github.com/amimof/node-cert-exporter/out/
    deploy:
      provider: releases
      api_key: ${GITHUB_API_KEY}
      file: 
      - node-cert-exporter-linux-amd64
      - node-cert-exporter-darwin-amd64
      - node-cert-exporter-linux-arm
      - node-cert-exporter-windows-amd64.exe
      skip_cleanup: true
      draft: true
      on:
        tags: true